MAKE_VARIABLES=BOOSTLIB CLP_PATH GDAL_HOME GLPK_HOME GTK_PATH JAGS_ROOT JAVA_HOME LIB_CURL LIB_FFTW LIB_GMP LIB_GSL LIB_JPEG LIB_MPFR LIB_SBML LIB_TIFF LIB_XML MYSQL_HOME NETCDF NLOPT_HOME QUANTLIB_ROOT SYMPHONY_HOME UDUNITS2_HOME

all: printvars

.PHONY: printvars
printvars:
	@$(foreach V,$(sort $(MAKE_VARIABLES)), $(if $(filter-out environment% default automatic, $(origin $V)),$(info $V=$($V) ($(value $V)))))
